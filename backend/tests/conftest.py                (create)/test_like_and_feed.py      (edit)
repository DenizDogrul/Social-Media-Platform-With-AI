pytest.ini
[pytest]
testpaths = tests

tests/conftest.py
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.database import Base

@pytest.fixture(scope='session')
def db_engine():
    engine = create_engine('sqlite:///:memory:')
    Base.metadata.create_all(engine)
    yield engine
    Base.metadata.drop_all(engine)

@pytest.fixture(scope='function')
def db_session(db_engine):
    connection = db_engine.connect()
    transaction = connection.begin()
    session = sessionmaker(bind=connection)()
    yield session
    session.close()
    transaction.rollback()
    connection.close()

tests/test_like_and_feed.py
import pytest

def test_like_duplicate_and_unlike_validation(db_session):
    # Setup code to create necessary users table and initial data
    db_session.execute("""
    CREATE TABLE users (
        id INTEGER PRIMARY KEY,
        username TEXT NOT NULL,
        email TEXT NOT NULL,
        password_hash TEXT NOT NULL
    )
    """)
    # Add test logic here
    assert True  # Replace with actual test logic

def test_get_post_likes_returns_users(db_session):
    # Setup code to create necessary users table and initial data
    db_session.execute("""
    CREATE TABLE users (
        id INTEGER PRIMARY KEY,
        username TEXT NOT NULL,
        email TEXT NOT NULL,
        password_hash TEXT NOT NULL
    )
    """)
    # Add test logic here
    assert True  # Replace with actual test logic

def test_feed_contains_likes_is_liked_and_ranking(db_session):
    # Setup code to create necessary users table and initial data
    db_session.execute("""
    CREATE TABLE users (
        id INTEGER PRIMARY KEY,
        username TEXT NOT NULL,
        email TEXT NOT NULL,
        password_hash TEXT NOT NULL
    )
    """)
    # Add test logic here
    assert True  # Replace with actual test logic